#include "MainMenu.h"
#include"Entity.h"

void MainMenu :: Load(){
    Entity* start_button = new Entity();
    Entity* quit_button = new Entity();

    //Engine::Get()->World.Add(start_button)      SINGLETON WORLD
    //Engine::Get()->World.Add(quit_button)
}